# Balpreet's Kitchen — Recipe Finder & Meal Planner

Two projects, matching the assignment's two deliverables:

- `recipe-ui-kit/` — the StencilJS component library (present at https://www.npmjs.com/package/@balpreet-nagarro/recipe-ui-kit)
- `recipe-finder/` — the SvelteKit app (consumes the published package)

