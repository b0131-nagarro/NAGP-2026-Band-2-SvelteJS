# recipe-finder

SvelteKit app for Balpreet's Kitchen Recipe Finder & Meal Planner assignment.
Consumes `@balpreet-nagarro/recipe-ui-kit` (StencilJS) as a published npm package —
never imported from source.
